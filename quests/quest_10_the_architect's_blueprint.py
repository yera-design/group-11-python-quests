#!/usr/bin/python3
length = float(input(" Enter the length of rectangle: "))
width = float(input(" Enter the width of a rectangle: "))
Area = length * width
print(f" the area of a rectengle is: {Area}")
